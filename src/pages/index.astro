---
const songs = await Astro.glob('../content/songs/*.md');
---

<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Библиотека песен</title>
  <style>
    body { font-family: Arial; padding: 20px; }
    .song { background: #f0f0f0; padding: 15px; margin: 10px 0; }
  </style>
</head>
<body>
  <h1>🎵 Библиотека песен</h1>
  
  {songs.length === 0 ? (
    <p>Пока нет песен. Добавьте первую!</p>
  ) : (
    songs.map(song => (
      <div class="song">
        <h3>{song.frontmatter.title}</h3>
        {song.frontmatter.artist && <p>Исполнитель: {song.frontmatter.artist}</p>}
        <a href={`/music-library/songs/${song.slug}`}>Открыть разбор</a>
      </div>
    ))
  )}
</body>
</html>
